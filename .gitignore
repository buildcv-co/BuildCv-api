# ---------- .NET ----------
**/bin/
**/obj/
*.user
*.suo
.vs/
TestResults/
coverage/
*.coverage
*.trx
*.binlog

# ---------- Secretos / API keys (NEVER commit) ----------
# Local config files with API keys
appsettings.Development.json
appsettings.*.local.json
appsettings.*.Production.json
appsettings.Local.json
appsettings.Staging.json
secrets.json
.env
.env.*
!.env.example
# User-secrets (per-developer, NOT in repo)
secrets/
*.user secrets

# ---------- Sistema ----------
.DS_Store
Thumbs.db

# ---------- Tooling de agentes ----------
.agents/
skills-lock.json
.claude/
.atl/
